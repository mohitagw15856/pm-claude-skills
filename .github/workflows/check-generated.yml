name: Check generated artifacts

# Skills are the single source of truth. The web index (web/skills.json) and the
# multi-platform exports (exports/) are generated from skills/*/SKILL.md. This
# job fails if either is out of date, so a skill edit can't ship without its
# regenerated artifacts.

on:
  pull_request:
    paths:
      - 'skills/**'
      - 'plugins/**'
      - 'web/build-skills.mjs'
      - 'scripts/build-exports.mjs'
      - 'exports/**'
      - 'web/skills.json'
      - 'web/skills-index.json'
      - 'skill-dupes-allow.json'
      - 'scripts/build-disambiguation.mjs'
  push:
    branches: [main]
    paths:
      - 'skills/**'
      - 'scripts/build-exports.mjs'
      - 'exports/**'
      - 'web/build-skills.mjs'
      - 'web/skills.json'
      - 'web/skills-index.json'
      - 'skill-dupes-allow.json'
      - 'scripts/build-disambiguation.mjs'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Verify multi-platform exports are up to date
        run: node scripts/build-exports.mjs --check

      - name: Verify web/skills.json is up to date
        run: |
          node web/build-skills.mjs
          git diff --exit-code -- web/skills.json web/skills-index.json \
            || (echo "::error::web/skills.json or web/skills-index.json is stale — run 'node web/build-skills.mjs' and commit." && exit 1)

      - name: Verify DISAMBIGUATION.md is up to date
        run: node scripts/build-disambiguation.mjs --check
